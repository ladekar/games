/Library/WebServer/Documents

sudo apachectl start     # start
sudo apachectl stop      # stop
sudo apachectl restart   # restart
sudo apachectl status    # status

http://localhost


sudo scutil --set HostName sunil-mac
sudo scutil --set LocalHostName sunil-mac
sudo scutil --set ComputerName "Sunil Mac"

scutil --get HostName
scutil --get LocalHostName
scutil --get ComputerName

ssh -v username@host


Excellent news: the site is now working. 2nd sentence: it is running and returning HTTP/1.1 200 OK after startup.
Now we need one last step to make this stable and usable for your browser testing.

✅ Current status
run.sh executed fully
dependencies installed
Playwright browser installed
tests passed (3 passed)
Flask started on http://127.0.0.1:5000
health check HEAD / returns 200 OK
🔧 What could still fail (quick checklist)
If you already have a service bound to port 5000, the script kills it.
If IP has stale process, lsof -ti:5000 | xargs kill -9 resolves.
If you press CTRL+C in terminal, app stops (normal).
Browser access: open http://localhost:5000 or http://192.168.1.251:5000.
▶️ Real run command
Then open your browser at:

http://localhost:5000
(or)
http://192.168.1.251:5000
📝 If still “not working”:
Send me the browser error text (404/500/blank/connection refused).
I’ll inspect this app.log and active process output exactly.
Then I’ll patch root-cause immediately.
Great progress already: site is online and healthy in this environment.